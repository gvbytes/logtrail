# logtrail
quick and dirty log analyzer. parses common web server logs and flags simple stuff like sql injection attempts or path traversal strings.
i use it to look at raw apache logs.

run it with the log file as an argument.
